startCommand:
  type: stdio
configSchema:
  type: object
  properties: {}
commandFunction: |-
  (config) => ({
    "command": "python",
    "args": [
      "scripts/living_map.py",
      "mcp"
    ],
    "env": {}
  })
