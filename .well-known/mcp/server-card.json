{
  "serverInfo": {
    "name": "living-codebase-map",
    "version": "2.5.0"
  },
  "authentication": {
    "required": false
  },
  "tools": [
    {
      "name": "update_map",
      "description": "Smart full scan or incremental update of PROJECT_MAP.md. Automatically detects file changes, calculates checksums, and updates modules.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "auto_commit": {
            "type": "boolean",
            "description": "Whether to auto-commit PROJECT_MAP.md to git after update."
          }
        }
      }
    },
    {
      "name": "check_drift",
      "description": "Ultra-fast drift detection in 0.02s using cached MD5 hash checking.",
      "inputSchema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "analyze_code_impact",
      "description": "Blast radius impact analysis for a symbol across the entire codebase graph.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "target": {
            "type": "string",
            "description": "Symbol, function, or route name to analyze."
          },
          "mode": {
            "type": "string",
            "description": "Analysis mode: 'lean' (token-saving, <15 lines) or 'deep' (full 6-tier ASCII tree).",
            "enum": ["lean", "deep"]
          }
        },
        "required": ["target"]
      }
    },
    {
      "name": "register_feature",
      "description": "Add or update a feature link in Module 5 (Feature Traceability Matrix) with smart NLP parsing.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "raw_text": {
            "type": "string",
            "description": "Natural language or structured feature specification to register."
          }
        },
        "required": ["raw_text"]
      }
    },
    {
      "name": "register_constraint",
      "description": "Add a new implicit architectural constraint or operational rule to Module 4.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "constraint": {
            "type": "string",
            "description": "The constraint or rule description to enforce."
          }
        },
        "required": ["constraint"]
      }
    },
    {
      "name": "get_map_summary",
      "description": "Read the ultra-compact lean architecture summary (PROJECT_MAP.min.md) to warmup context without wasting tokens.",
      "inputSchema": {
        "type": "object",
        "properties": {}
      }
    }
  ],
  "resources": [],
  "prompts": []
}
